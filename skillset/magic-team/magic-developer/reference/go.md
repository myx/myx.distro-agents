# Go

Starter stub — not yet populated from real estate knowledge. Fill in as genuine language-level axioms (not project-specific conventions) surface from real work, e.g. from the relevant `partner-*`'s `*-go-daemon`/`*-go-service`/`*-go-library`/`*-go-client` work.
