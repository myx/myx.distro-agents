# Java

Starter stub — not yet populated from real estate knowledge. Fill in as genuine language-level axioms (not project-specific conventions) surface from real work, e.g. from the relevant `partner-*`'s `*-ae3-service`/`*-ae3-library` work or a `keeper-*`'s daily archaeology in the legacy EclipseWorkspace tree.
